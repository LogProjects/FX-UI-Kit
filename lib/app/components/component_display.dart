import 'package:flutter/material.dart';

class ComponentDisplay {
  final Widget component;
  final double? height;
  final double? width;

  ComponentDisplay({required this.component, this.height, this.width});
}